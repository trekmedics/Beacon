<%= form_for(@data_center) do |f| %>
  <% if @data_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@data_center.errors.count, "error") %> prohibited this data_center from being saved:</h2>
      <ul>
      <% @data_center.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form">
    <%= f.label :name %>
    <%= f.text_field :name, { class: :text_field } %>
    <%= f.check_box :is_simulator, { class: :check_box } %>
    <%= f.label :is_simulator %>
    <%= f.submit %>
  </div>
<% end %>
